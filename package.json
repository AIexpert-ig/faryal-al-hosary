{
  "name": "faryal-al-hosary",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
